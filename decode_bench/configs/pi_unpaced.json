{
  "_comment": "Full-speed (race-to-idle) decode ΔW, Pi 5. Companion to pi_sw_matrix (realtime-paced). Needed because energy-per-video-minute under race-to-idle must use power measured AT full speed, not paced power x unpaced duration. -stream_loop -1 keeps the decoder saturated for the whole window.",
  "name": "pi_unpaced",
  "meter_ip": "192.168.1.146",
  "cadence_s": 1.5,
  "baseline_samples": 15,
  "settle_s": 10,
  "startup_skip_s": 6,
  "window_s": 90,
  "gap_s": 10,
  "device": {"type": "ssh", "host": "192.168.1.102", "user": "admin"},
  "runs": [
    {"name": "bbb_h264_full", "cmd": "ffmpeg -nostdin -stream_loop -1 -i /dev/shm/decode/bbb_h264_6min.mp4 -f null - ", "stop_cmd": "pkill -f ffmpeg"},
    {"name": "bbb_hevc_full", "cmd": "ffmpeg -nostdin -stream_loop -1 -i /dev/shm/decode/bbb_h265_6min.mp4 -f null - ", "stop_cmd": "pkill -f ffmpeg"},
    {"name": "bbb_av1_full",  "cmd": "ffmpeg -nostdin -stream_loop -1 -i /dev/shm/decode/bbb_av1_6min.mp4 -f null - ",  "stop_cmd": "pkill -f ffmpeg"}
  ]
}
